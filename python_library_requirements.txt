# CI pipeline dependencies for run_horizon_scanner.yml.
# Keep in sync with pyproject.toml (which is used for local Poetry installs).
# Packages listed here but absent from pyproject.toml are noted below.

# --- LLM providers & clients ---
google-generativeai>=0.8,<1
openai>=1.57,<2
httpx>=0.27,<1

# --- Core libraries ---
python-dotenv>=1.0,<2
duckdb>=1.1,<2
pandas>=2.2,<3
PyYAML>=6.0,<7
requests>=2.32,<3
numpy>=2.3,<3
pydantic>=2.6,<3
python-docx>=1.1,<2
pdfplumber>=0.10,<1
beautifulsoup4>=4.12,<5

# --- API server (used by pythia/api) ---
fastapi>=0.110,<1
uvicorn[standard]>=0.30,<1

# --- Geospatial / NMME ingestion ---
xarray>=2024.1,<2026
netCDF4>=1.6,<2
regionmask>=0.12,<1
geopandas>=1.0,<2
shapely>=2.0,<3
pycountry>=24.6,<25
