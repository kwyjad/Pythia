enabled: true
api:
  admin_levels: [admin0, admin1, admin2]
  countries: []  # optional hint; discovery via SDK always fetches the full catalog
output:
  measure: stock
field_aliases:
  idp_count: ["TotalIDPs", "IDPTotal"]
