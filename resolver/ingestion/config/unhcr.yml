enabled: true
base_url: "https://api.unhcr.org/population/v1/"
user_agent: "forecaster-resolver/1.0 (github.com/oughtinc/Pythia)"
window_days: 60
page_limit: 5000

endpoints:
  asylum_applications: "population/"

params:
  cf_type: "ISO"
  coo_all: "true"
  coa_all: "true"
  granularity: "month"   # month|year
  years_back: 3
  include_years: []

paging:
  max_pages: 2

debug:
  debug_every: 10

defaults:
  max_results: 20000

test_iso3: ""
