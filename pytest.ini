[pytest]
addopts = -ra --durations=25
timeout = 180
timeout_method = thread
markers =
    allow_network: tests that intentionally make network calls
    db: tests that require duckdb/database backend
    nightly: long-running/nightly tests
    slow: slow tests
